<div class="actionbloc col-lg-1 ">

<%= link_to 'Show', post %>
<% if post.user_id == current_user.id || current_user.role == 1%>
<%= link_to 'Edit', edit_post_path(post) %>
<%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %>
<%end%>
</div>
